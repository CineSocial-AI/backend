// TODO: Update with actual backend port when known and use environment variables for production
const API_BASE_URL = 'http://localhost:5000/api'; // Assuming 5000, replace if different. For ASP.NET Core, it's often 5001 or 7xxx for HTTPS.

export default API_BASE_URL;
