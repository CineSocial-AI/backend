using MediatR;

namespace CineSocial.Api.GraphQL.Queries;

public class Query
{
    // Queries are organized by feature areas
    // Each resolver method will use MediatR to execute the corresponding query handler
}
